# ALFWorld Preset (text-world simulator)

Mock embodied tasks compatible with SkillOpt optimization loop.

```bash
cd benchmarks/alfworld
skillopt optimize config.yaml
skillopt baselines config.yaml
```

Strong skill should include procedural keywords: `go to`, `pick up`, `heat`, `put`, `clean`, `step-by-step`.
